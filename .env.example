# Shared
# Required: generate with `openssl rand -hex 32`
JWT_SECRET=
ALLOWED_EMAIL_DOMAIN=vanderbilt.edu
# Comma-separated list of allowed CORS origins (update for production)
BACKEND_CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Backend
DATABASE_URL=postgresql://rmdh_user:rmdh_password@db:5432/rmdh
POSTGRES_USER=rmdh_user
POSTGRES_PASSWORD=rmdh_password
POSTGRES_DB=rmdh

# Frontend
VITE_API_BASE_URL=http://localhost:8000
